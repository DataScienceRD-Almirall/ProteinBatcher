% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Step1_Filter_Imputation.R
\name{filter_se_missing}
\alias{filter_se_missing}
\title{Filter proteins by missingness within conditions (SummarizedExperiment)}
\usage{
filter_se_missing(se, percentage = 50)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} (or compatible) object with:
\itemize{
\item an assay matrix accessible via \code{assay(se)} (numeric, may contain \code{NA})
\item \code{colData(se)$condition} (character or factor) giving the experimental
condition per sample.
\item (optional) \code{rowData(se)$Protein.Names} and \code{rowData(se)$Genes} used to
annotate the returned table of removed proteins. If missing, they will be
safely filled with \code{NA_character_}.
}}

\item{percentage}{Numeric scalar in \verb{[0, 100]}. Minimum proportion of
\strong{non-missing} values required within a condition for a protein to be
retained. Default: \code{50} (i.e., at least half of samples observed within
any one condition).}
}
\value{
A named list with two elements:
\describe{
\item{\code{se_filt}}{\code{SummarizedExperiment} containing only retained proteins.}
\item{\code{removed}}{A \code{data.frame} with metadata for proteins that were
filtered out, columns: \code{protein_id}, \code{gene_name}, and \code{reason}.}
}
}
\description{
\code{filter_se_missing()} removes proteins (rows) that fail a per-condition
missingness threshold. For each condition in \code{colData(se)$condition}, the
function computes the \strong{proportion of non-missing intensities per protein}
and keeps a protein if it meets the threshold in \strong{at least one} condition.

This is useful in label-free proteomics pipelines prior to imputation and
differential analysis, ensuring that proteins are not uniformly sparse across
all conditions.
}
\details{
Let \eqn{X} be the assay matrix (\code{assay(se)}), rows = proteins and columns =
samples. For every distinct \code{condition} in \code{colData(se)$condition}, we
compute \code{rowMeans(!is.na(X[, condition == cn]))}. A protein is \strong{kept} if
the per-condition proportion of observed values is \verb{>= percentage / 100} for
\strong{any} condition. Otherwise, it is \strong{removed}.
}
\section{Notes}{

\itemize{
\item Samples with \code{NA} in \code{colData(se)$condition} are \strong{excluded} from
per-condition computations and a message is emitted.
\item If \code{percentage = 0}, all proteins with at least one observed value in any
condition are kept. If \code{percentage = 100}, only proteins fully observed
within at least one condition are kept.
}
}

\examples{
library(SummarizedExperiment)

## Toy expression matrix: 4 proteins x 4 samples
mat <- matrix(
  c(
    1,  NA,  2,  3,   # p1: OK in cond B
    NA, NA,  NA, NA, # p2: always missing -> removed
    5,  6,   NA, NA, # p3: OK in cond A
    NA, NA,  1,  NA  # p4: too sparse everywhere
  ),
  nrow = 4, byrow = TRUE,
  dimnames = list(
    paste0("p", 1:4),
    paste0("s", 1:4)
  )
)

## Sample conditions
cd <- DataFrame(
  condition = c("A", "A", "B", "B"),
  row.names = colnames(mat)
)

## Protein annotations (optional)
rd <- DataFrame(
  Protein.Names = paste0("Prot", 1:4),
  Genes = paste0("Gene", 1:4),
  row.names = rownames(mat)
)

se <- SummarizedExperiment(
  assays = list(intensity = mat),
  colData = cd,
  rowData = rd
)

## Keep proteins with >= 50\% observed values in at least one condition
out <- filter_se_missing(se, percentage = 50)

## Filtered SummarizedExperiment
out$se_filt

## Table of removed proteins with reasons
out$removed

}
\seealso{
\itemize{
\item \code{SummarizedExperiment::SummarizedExperiment}
\item Downstream steps like \code{impute_se()} for imputation.
}
}
