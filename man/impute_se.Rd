% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Step1_Filter_Imputation.R
\name{impute_se}
\alias{impute_se}
\title{Impute missing proteomics intensities within conditions
(SummarizedExperiment)}
\usage{
impute_se(
  se,
  threshold = 0.5,
  ldv_source = c("global", "per_condition"),
  sd_fallback = NULL,
  lower_bound = NULL
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} with numeric assay and
\code{colData(se)$condition}.}

\item{threshold}{Numeric in \verb{(0,1]}, default \code{0.5}.}

\item{ldv_source}{\code{c("global","per_condition")}, default \code{"global"}.}

\item{sd_fallback}{Optional numeric fallback SD; default is median non‑zero
SD.}

\item{lower_bound}{Optional minimum clip for imputed values (avoid on log
scale).}
}
\value{
A \code{SummarizedExperiment} with imputed assay, updated
metadata/rowData.
}
\description{
\code{impute_se()} imputes missing values using a two‑step, condition‑aware
strategy: (1) within‑condition mean when missingness < \code{threshold}; (2)
left‑censored draws (LDV) for remaining NAs, using a low‑intensity reference
and per‑protein SD.
}
\details{
Let \code{assay(se)} be a numeric matrix (rows = proteins, cols = samples);
\code{colData(se)$condition} provides the condition for each sample. Step (1)
replaces NAs in a condition with the mean of observed values for that protein
in the same condition when the NA proportion is below \code{threshold}. Step (2)
replaces remaining NAs with \code{rnorm(1, mean = LDV, sd = sd_protein)}, where
LDV is the global (or per‑condition) minimum observed intensity; per‑protein
SDs are computed from available values with a robust fall-back when needed.

The per‑cell imputation method is recorded in \code{metadata(se)$imputation_map}
(\code{"none"|"mean"|"ldv"}). A row‑level flag is added at \code{rowData(se)$imputed}.
}
\examples{
library(SummarizedExperiment)

## Toy matrix with missing values
mat <- matrix(
  c(
    10, 11,  NA, NA,  # p1: partial missing
    NA, NA,  NA, NA, # p2: fully missing
    5,  NA,  6,  7   # p3: mixed
  ),
  nrow = 3, byrow = TRUE,
  dimnames = list(
    paste0("p", 1:3),
    paste0("s", 1:4)
  )
)

## Conditions for samples
cd <- DataFrame(
  condition = c("A", "A", "B", "B"),
  row.names = colnames(mat)
)

## Build SummarizedExperiment
se <- SummarizedExperiment(
  assays = list(intensity = mat),
  colData = cd
)

## Impute with default strategy:
## - mean within condition if missingness < threshold
## - LDV otherwise
set.seed(123)
se_imp <- impute_se(se, threshold = 0.5, ldv_source = "global")

## Imputed assay
assay(se_imp)

## Per-cell imputation method
metadata(se_imp)$imputation_map

## Row-level flag: was any value imputed?
rowData(se_imp)$imputed

}
\seealso{
SummarizedExperiment::SummarizedExperiment
}
